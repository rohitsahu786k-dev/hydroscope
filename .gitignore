node_modules
.next
.next-dev*
dist
.env
.env.local
.env.*.local
coverage
*.log
*.tsbuildinfo
.vercel
.turbo
.codex-screens

# Review documents dropped in the repo root. Kept out of history: the last one
# was 11 MB of embedded screenshots, and it is a record of feedback rather
# than source the site builds from.
/*.docx

# Deployment bundle assembled by tools/bundle-standalone.mjs
/deploy
